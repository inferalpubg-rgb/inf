# 🎨 Zoved - Редизайн с Glassmorphism

## 🌟 Новые фичи

### 1. **Glassmorphism Эффект (Размытое Стекло)**
- Применен современный UI паттерн "размытого стекла" (liquid glass)
- Полупрозрачные контейнеры с backdrop-filter blur эффектом
- Плавающая анимация элементов
- Совместимость со всеми современными браузерами

### 2. **Фон с Морем**
- Красивый градиент синего цвета, имитирующий морскую воду
- Анимированные волны на фоне
- Легкий радиальный градиент для глубины
- Все элементы интерфейса остаются видимыми благодаря glassmorphism

### 3. **Анимированный Эмоджи 18+**
- Анимированный эмоджи 🔞 на каждой странице верификации
- Двойная анимация: bounce (подпрыгивание) + glow (сияние)
- Плавные переходы и трансформации

### 4. **Многоязычность (i18n)**
Поддержка 5 языков с переключателем в правом верхнем углу:

- 🇷🇺 **Русский** (ru) - Default
- 🇬🇧 **Английский** (en)
- 🇨🇳 **Китайский** (zh)
- 🇯🇵 **Японский** (ja)
- 🇰🇷 **Корейский** (ko)

#### Как работает система перевода:
- Все текстовые строки хранятся в `static/languages.json`
- Переключатель языка в левом верхнем углу (флаги)
- Выбранный язык сохраняется в `localStorage`
- Все элементы с `data-i18n` атрибутом автоматически переводятся

#### Как добавить новый текст:
1. Добавьте ключ в `static/languages.json` для каждого языка
2. Используйте `data-i18n="key"` в HTML элементах
3. Для input placeholder: `data-i18n-attr="key:placeholder"`

### 5. **Современный UI/UX**
- Адаптивный дизайн (мобильные, планшеты, десктопы)
- Плавные переходы и анимации (0.3s ease transitions)
- Иконки кнопок переключения языка (флаги эмоджи)
- Красивые сообщения об ошибках
- Спиннер загрузки при отправке формы
- Эффект парящего стекла (float animation)

## 📁 Структура файлов

```
edprog/
├── static/
│   ├── styles.css          # Глобальные стили с glassmorphism
│   ├── i18n.js            # Система интернационализации
│   ├── languages.json     # Словарь переводов для 5 языков
│   ├── duck.json          # Анимация утки (TGS)
│   └── telegram-logo.svg  # Логотип Telegram
├── templates/
│   ├── base.html          # Базовый шаблон
│   ├── auth_start.html    # Главная страница (фон моря + glassmorphism)
│   ├── auth.html          # Ввод номера телефона
│   ├── code.html          # Ввод кода подтверждения
│   ├── password.html      # Ввод 2FA пароля
│   └── success.html       # Страница успеха
├── app.py                 # Flask приложение
├── bot.py                 # Telegram бот
├── config.py              # Конфигурация
├── telegram_client.py     # Telegram клиент
└── run.py                 # Точка входа (запускает Flask + Bot)
```

## 🎯 Главные стили (styles.css)

### Ключевые классы:
- `.glass` - Контейнер с glassmorphism эффектом
- `.auth-container` - Главный контейнер с позиционированием
- `.emoji-18plus` - Анимированный эмоджи
- `.language-switcher` - Переключатель языков
- `.lang-btn` - Кнопка выбора языка
- `.btn` - Кнопка действия с градиентом
- `.input-field` - Поле ввода с glassmorphism эффектом
- `.error-message` - Сообщение об ошибке
- `.success-container` - Контейнер успеха

### Анимации:
- `bounce` - Подпрыгивание эмоджи
- `glow` - Сияние эмоджи
- `seaWave` - Волны на фоне моря
- `glassFloat` - Парящее движение glassmorphism контейнера
- `spin` - Вращение спиннера загрузки
- `slideIn` - Появление сообщений об ошибке

## 🚀 Использование

### На Replit:
1. Код автоматически перезагружается при изменении
2. Откройте проект и нажмите "Run"
3. Сайт будет доступен по URL: `https://zoved-site-maker--[username].replit.app`

### Локально:
```bash
cd edprog
python3 run.py
# Откройте http://localhost:3000
```

## 🌐 Смена языка

Пользователь может переключить язык:
1. Нажав на флаг в правом верхнем углу
2. Язык сохранится в браузере (localStorage)
3. При следующем визите сайт будет на выбранном языке

## 💡 Возможность расширения

### Для добавления нового языка:
1. Добавьте новый код языка в `static/languages.json`
2. Добавьте кнопку в `static/i18n.js` (languageNames объект)
3. Перезагрузите страницу

### Для добавления новых переводов:
1. Откройте `static/languages.json`
2. Добавьте новый ключ-значение для каждого языка
3. Используйте ключ в HTML через `data-i18n="ключ"`

## 🎨 Цветовая схема

- **Фон морской воды**: Градиент от `#0f2c7d` до `#2d8fb5`
- **Glassmorphism**: `rgba(255, 255, 255, 0.1)` с `blur(20px)`
- **Активные элементы**: Голубой акцент `#64c8ff`
- **Текст**: Белый с полупрозрачностью для иерархии
- **Ошибки**: Красный `#ff6464` с низкой полупрозрачностью

## 📱 Адаптивность

Все элементы полностью адаптивны:
- На мобильных: padding уменьшено, размеры оптимизированы
- На планшетах: средние размеры
- На десктопах: полный размер с красивой центровкой

## ✨ Бонусные фичи

1. **Плавные переходы**: Все состояния имеют smooth transitions
2. **Hover эффекты**: Кнопки и ссылки реагируют на наведение
3. **Фокус эффекты**: Input поля светятся при фокусе
4. **Валидация форм**: Встроенная HTML5 валидация
5. **Парящие элементы**: Glassmorphism блоки немного подпрыгивают
6. **Спиннер загрузки**: Красивая анимация при отправке формы

---

**Дата обновления**: 19 января 2026  
**Версия**: 2.0 (Redesign)  
**Статус**: ✅ Production Ready
